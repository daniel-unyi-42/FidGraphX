dataset: "BAMotifs"
data_path: "data"
log_path: "logs"

task_type: "classification"

conv_type: "GINConv"

num_layers: 2
hidden_channels: 64
use_norm: false
learning_rate: 0.002
epochs: 200
batch_size: 64

train_baseline: true
train_predictor: true
train_selector: true

sparsity: 0.2
reward_coeff: 1.0

train_size: 0.8
val_size: 0.1
test_size: 0.1

seed: 0
print_results: true
